---
resource_types:
  - name: google-cloud-storage
    type: docker-image
    source:
      repository: frodenas/gcs-resource

resources:
- name: pal-tracker
  type: git
  source:
    uri: {{github-repo}}
    branch: my-work
    private_key: {{github-private-key}}

- name: version
  type: semver
  source:
    bucket: {{gcs-bucket}}
    endpoint: storage.googleapis.com
    key: pal-tracker-distributed/version
    access_key_id: {{gcs-access-key-id}}
    secret_access_key: {{gcs-secret-access-key}}

- name: allocations-artifacts
  type: google-cloud-storage
  source:
    bucket: {{gcs-bucket}}
    regexp: releases/pal-tracker-distributed/allocations-server-(.*).jar
    json_key: {{gcs-json}}

- name: backlog-artifacts
  type: google-cloud-storage
  source:
    bucket: {{gcs-bucket}}
    regexp: releases/pal-tracker-distributed/backlog-server-(.*).jar
    json_key: {{gcs-json}}

- name: registration-artifacts
  type: google-cloud-storage
  source:
    bucket: {{gcs-bucket}}
    regexp: releases/pal-tracker-distributed/registration-server-(.*).jar
    json_key: {{gcs-json}}

- name: timesheets-artifacts
  type: google-cloud-storage
  source:
    bucket: {{gcs-bucket}}
    regexp: releases/pal-tracker-distributed/timesheets-server-(.*).jar
    json_key: {{gcs-json}}

- name: cf
  type: cf
  source:
    api: {{cf-url}}
    username: {{cf-username}}
    password: {{cf-password}}
    organization: {{cf-org}}
    space: sandbox
    skip_cert_check: true


jobs:
- name: build
  plan:
  - get: pal-tracker
    trigger: true
  - get: version
    params:
      bump: patch
  - task: build and test
    file: pal-tracker/ci/build.yml
  - put: allocations-artifacts
    params:
      file: build-output/allocations*.jar
  - put: backlog-artifacts
    params:
      file: build-output/backlog*.jar
  - put: registration-artifacts
    params:
      file: build-output/registration*.jar
  - put: timesheets-artifacts
    params:
      file: build-output/timesheets*.jar
  - put: version
    params:
      file: version/number

- name: deploy-allocations
  plan:
  - get: pal-tracker
    passed: [build]
    trigger: true
  - get: allocations-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker/manifest-allocations.yml
      path: allocations-artifacts/allocations-server*.jar
      current_app_name: tracker-allocations

- name: deploy-backlog
  plan:
  - get: pal-tracker
    passed: [build]
    trigger: true
  - get: backlog-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker/manifest-backlog.yml
      path: backlog-artifacts/backlog-server*.jar
      current_app_name: tracker-backlog

- name: deploy-registration
  plan:
  - get: pal-tracker
    passed: [build]
    trigger: true
  - get: registration-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker/manifest-registration.yml
      path: registration-artifacts/registration-server*.jar
      current_app_name: tracker-registration

- name: deploy-timesheets
  plan:
  - get: pal-tracker
    passed: [build]
    trigger: true
  - get: timesheets-artifacts
    trigger: false
  - put: cf
    params:
      manifest: pal-tracker/manifest-timesheets.yml
      path: timesheets-artifacts/timesheets-server*.jar
      current_app_name: tracker-timesheets
